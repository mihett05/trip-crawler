/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetCitiesQueryResponse, GetCities500 } from '../types/GetCities.ts';
import type { Client, RequestConfig, ResponseErrorConfig } from '@kubb/plugin-client/clients/axios';
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query';
import { getCities } from '../clients/getCities.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const getCitiesQueryKey = () => [{ url: '/v1/cities' }] as const;

export type GetCitiesQueryKey = ReturnType<typeof getCitiesQueryKey>;

export function getCitiesQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const queryKey = getCitiesQueryKey();
  return queryOptions<
    GetCitiesQueryResponse,
    ResponseErrorConfig<GetCities500>,
    GetCitiesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getCities({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Get availablt cities
 * @summary Get cities
 * {@link /v1/cities}
 */
export function useGetCities<
  TData = GetCitiesQueryResponse,
  TQueryData = GetCitiesQueryResponse,
  TQueryKey extends QueryKey = GetCitiesQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetCitiesQueryResponse,
        ResponseErrorConfig<GetCities500>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getCitiesQueryKey();

  const query = useQuery(
    {
      ...getCitiesQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetCities500>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
