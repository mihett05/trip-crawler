apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls
  namespace: traefik
spec:
  secretName: grafana-tls
  dnsNames:
    - grafana.letsgotrip.store
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer

