type: object
required:
  - code
  - message
properties:
  code:
    type: string
    description: Error code
    enum:
      - INVALID_INPUT
      - INTERNAL_ERROR
  message:
    type: string
    description: Full error message
