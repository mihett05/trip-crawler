apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tripcrawler.fullname" . }}-test-connection"
  labels:
    {{- include "tripcrawler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tripcrawler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
